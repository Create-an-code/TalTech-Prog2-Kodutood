# https://leetcode.com/problems/truncate-sentence/
class Solution:
    def truncateSentence(self, s, k):
        #vastus = s.split(' ')[:k]
        #vastus = ' '.join(vastus)
        #return vastus
        return ' '.join(s.split(' ')[:k])
